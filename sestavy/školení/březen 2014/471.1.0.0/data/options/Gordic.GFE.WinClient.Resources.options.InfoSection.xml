<StandardProperties version="4.72.1.7">
  <Property name = "InfoSection.GRF">
    <info
      ixs_xme="${StructureID}"
      xmeta_ver="${StructureVersionMajor}"
      xmeta_subver_min="${StructureVersionMinor}"
      Structure_Parameters="${Structure_Parameters}"/>
  </Property>
  <Property name = "InfoSection.GRR">
    <info
      ixs_xme="${StructureID}"
      xmeta_ver="${StructureVersionMajor}"
      xmeta_subver_min="${StructureVersionMinor}"
      Structure_Parameters="${Structure_Parameters}"/>
  </Property>
  <Property name = "InfoSection.RTF">
    <info
      ixs_xme="${StructureID}"
      xmeta_ver="${StructureVersionMajor}"
      xmeta_subver_min="${StructureVersionMinor}"
      Structure_Parameters="${Structure_Parameters}"/>
  </Property>
  <Property name = "InfoSection.MSE">
    <info
      ixs_xme="${StructureID}"
      xmeta_ver="${StructureVersionMajor}"
      xmeta_subver_min="${StructureVersionMinor}"
      Structure_Parameters="${Structure_Parameters}"/>
  </Property>
  <Property name = "InfoSection.DOCFRM">
    <info
      ixs_xme="${StructureIxsXme}"
      xmeta_ver="${StructureVersionMajor}"
      xmeta_subver_min="${StructureVersionMinor}"
      parts="${StructureParts}"
      ixs_typ="${StructureIxsType}"
      nazev="${StructureNazev}"
      poznamka="${StructurePoznamka}"
      ixs_alv="${StructureIxsAlv}"
      rokmes_od="${StructureRokmesOd}"
      rokmes_do="${StructureRokmesDo}"
      form_vyst="${StructureFormVyst}"
      />
  </Property>
  <Property name = "InfoSection.XME">
    <info
      ixs_xme="${StructureIxsXme}"
      xmeta_ver="${StructureVersion}"
      xmeta_subver="${StructureSubVersion}"
      xmeta_subver_min="${StructureSubVersionMinor}"
      ixs_typ="${StructureIxsType}"
      ixs_alv="${StructureIxsAlv}"
      parts="${StructureParts}"
      maker="${StructureMaker}"
      nazev="${StructureNazev}"
      poznamka="${StructurePoznamka}"
      rokmes_od="${StructureRokmesOd}"
      rokmes_do="${StructureRokmesDo}"
      valid_from="${StructureValidFrom}"
      valid_to="${StructureValidTo}"
      />
  </Property>
</StandardProperties>
