Readers.GAdtReaderVerzeDB = {
	columns: ["verze_db"],	// keys: "verze_db"
	rowSize: 100,
	readAll: false,
	permanent: false
}
Fields.gAdtReaderVerzeDB = {
	itemTemplate: "{verze_db}",
	helperItemTemplate: "{verze_db}",
	helperColumns: ["verze_db"]
}
